export const DEFAULT_SETTINGS = {
  tripwireX: 50,
  tripwireY: 50,
  detectionWidth: 15,
  detectionHeight: 15,
  sensitivity: 25,
  cooldown: 1000,
  showTimerOverlay: false,
  targetLaps: 0,
};

export const MAX_HISTORY_LAPS = 100;